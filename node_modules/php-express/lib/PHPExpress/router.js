module.exports = function(req, res) {
    req.on('data', function(post) {
	req.body = post.toString();
    });
    req.on('end', function() {
	res.render(req.path.slice(1), {
	    method: req.method,
	    get: req.query,
	    post: req.body
	});
    });
};
